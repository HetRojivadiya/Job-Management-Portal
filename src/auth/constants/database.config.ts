export const DatabaseConfig = {
  DIALECT: 'postgres',
  HOST: 'localhost',
  PORT: 5432,
  USERNAME: 'postgres',
  PASSWORD: 'het123',
  DATABASE: 'Job Management Portal',
  AUTO_LOAD_MODELS: true,
  SYNCHRONIZE: true,
} as const;
